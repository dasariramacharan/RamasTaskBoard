import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'my-milestones-root',
    template: `My Milestones
    <router-outlet></router-outlet>
    `
})
export class MilestonesComponent {}