// Place your settings in this file to overwrite default and user settings.
{

      // Configure glob patterns for excluding files and folders.
    "files.exclude": {
        "**/.git": true,
        "**/.svn": true,
        "**/.DS_Store": true,
         "**/**.js.map": true,
         "**/**.js": true
    },

    "typescript.tsdk": "../RamasTaskBoard/node_modules/typescript/lib",

    // Adjust the zoom level of the window. 
    //The original size is 0 and each increment above (e.g. 1)
    // or below (e.g. -1) represents zooming 20% larger or smaller. 
    //You can also enter decimals to adjust the zoom level with a finer granularity.
   "window.zoomLevel": 0.3

}